interface:
  display_name: "Omniverse USD Asset Validator"
  short_description: "Validate USD assets by scenario-aware rules"
  default_prompt: "Use $omniverse-usd-asset-validator to validate this USD asset through `python omni_asset_cli.py validate` or `python omni_asset_cli.py validate-from-prompt`. First identify whether it should be treated as a static, collidable, or movable asset, then explain why the selected rules fit that scenario."
