interface:
  display_name: "Omni Asset CLI Agent"
  short_description: "Guide agents through omni-asset-cli workflows"
  default_prompt: "Use $omni-asset-cli-agent to validate OpenUSD assets with omni_asset_cli.py, run Linux + Isaac Sim Docker runtime evidence when requested, prefer the running isaac-sim container in this workspace, and explain validator, PhysX contact, rendered PNG, and MP4 artifacts."
